import { urlConfig } from "./config";

async function loginRq() {
    fetch(
        urlConfig, 
        {
            headers: {

            },
            body: JSON.stringify({
                
            })
        }
    ).then(() => {
        return({});
    });
}

const registerRq = () => {

}