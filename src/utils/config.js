const baseUrl = "http://localhost:8080"

export const urlConfig = {
    formatsUrl: `${baseUrl}/formats`,
    logUrl: `${baseUrl}/log`,
    loginUrl: `${baseUrl}/login/authorize`,
    registerUrl: `${baseUrl}/login/register`
}